<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="false"
        tools:replace="android:allowBackup"
        android:requestLegacyExternalStorage="true">
        <activity
            android:launchMode="singleTask"
            android:name="com.unity3d.player.UnityPlayerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="com.oculus.intent.category.VR"/>
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity>
        <!-- Alias that launches into flat/monoscopic mode without VR -->
        <activity-alias
            android:name=".MonoscopicModeActivity"
            android:targetActivity="com.unity3d.player.UnityPlayerActivity"
            android:label="@string/alias_monoscopic_label"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data android:name="EnableMonoscopicMode" android:value="true" />
        </activity-alias>

        <!-- Alias that launches with XR/VR disabled -->
        <activity-alias
            android:name=".DisableXrModeActivity"
            android:targetActivity="com.unity3d.player.UnityPlayerActivity"
            android:label="@string/alias_noxr_label"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data android:name="DisableXrMode" android:value="true" />
        </activity-alias>

        <meta-data android:name="unityplayer.SkipPermissionsDialog" android:value="false" />
        <meta-data android:name="pvr.app.type" android:value="vr" />
    </application>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.oculus.permission.USE_SCENE" />
</manifest>
